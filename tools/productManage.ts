export class Product{
    title: string
    description: string
    price: string
    constructor(title,desc,price){
        this.description=desc
        this.price=price
        this.title=title
    }
}